{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "cd /app/backend && (node ../node_modules/prisma/build/index.js db push --accept-data-loss || echo 'db push failed, continuing') && (node ../node_modules/tsx/dist/cli.mjs prisma/seed.ts || echo 'seed failed, continuing') && npm start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}